<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cascade.com.cascade_app" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PasswordListActivity"
            android:label="@string/title_activity_password_list" >
        </activity>
        <activity
            android:name=".LinkAccountSource"
            android:label="@string/title_activity_authentification_source" >
        </activity>
        <activity
            android:name=".AuthenticationSource"
            android:label="@string/title_activity_authetication_source" >
        </activity>
    </application>

</manifest>
